test "join multiple channels" do
  assert true
end

test "send message across channel" do
  assert
end

test "attach body signature to message" do
  assert
end

test "receive message in common channel" do
  assert
end

test "tag notification priority" do
  assert priority == priority
end

test "start a new BEAM node" do
  assert
end

test "pass message through BEAM node through other sender" do
  assert
end

test "be a pretty interface" do
  assert
end

test "create message template" do
  assert
end
